using System;
namespace BoshokuDemo1.Helper
{
    public enum OrderChild
    {
        createdDate,
        startAt,
        endAt,
        startAndEndAt,
        key,
        keyAndEndAt,
        keyAndStartAt
    }
    public enum CurrentTabPageChange{

        pageAnnoun,
        pageActivity
    }
}
